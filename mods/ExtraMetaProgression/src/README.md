# Source files for Extra Meta Progression go here

The compiled `ExtraMetaProgression.dll` is in the parent directory. To rebuild from source, drop your `.cs` files here and run:

```bash
./build/build.sh ExtraMetaProgression
```

If you don't have source files yet, you can decompile the DLL with [ILSpy](https://github.com/icsharpcode/ILSpy) or [dnSpy](https://github.com/dnSpyEx/dnSpy) and check the result back in here.
